<h1>ユーザ一覧</h1>

<ul class="users">
  <% @users.each do |user| %>
    <li>
      <%= image_tag display_avatar(user), size: "#{'50' if display_avatar(user).is_a?(String) }" %>
      <%= link_to user.name, user %>
    </li>
  <% end %>
</ul>

<%= paginate @users %>