<div id="like_button">
  <% if !Like.exists?(post_id: @post.id, user_id: current_user.id) %>
    <%= render 'likes/like' %>
  <% else %>
    <%= render 'likes/unlike' %>
  <% end %>
</div>

<div id="like_count">
  <%= @post.likes.count %>
</div>