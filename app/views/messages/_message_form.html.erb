<%= form_with(model: @message, local: false) do |f| %>
  <%= f.text_field :content, :placeholder => "メッセージを入力" , :size => 70, id: "message_text" %>
  <%= f.hidden_field :room_id, :value => @room.id %>
  <br>
  <%= f.submit "送信" %>
<% end %>