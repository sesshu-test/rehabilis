<div id="like_button">
  <% if !Like.exists?(post_id: @post.id, user_id: current_user.id) %>
    <%= render 'like' %>
  <% else %>
    <%= render 'dislike' %>
  <% end %>
</div>

<div id="like_count">
  <%= @post.likes.count %>
</div>