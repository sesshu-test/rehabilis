<h1>投稿一覧</h1>

<div id="category-tab">
  <%= link_to "タイムライン", root_path(name: "timeline"), remote: true if user_signed_in? %>
  <%= link_to "全て", root_path(name: "all"), remote: true %>
  <% @hashtags.each do |hashtag| %>
    <%= link_to "##{hashtag.name} #{hashtag.posts.count}件", root_path(name: hashtag.name), remote: true %>
  <% end %>
</div>

<div class="posts">
  <%= render 'shared/posts' %>
</div>
